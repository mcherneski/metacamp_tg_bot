version: 0.2

phases: 
  install:
    runtime-versions:
      nodejs: 22.1.0
    commands:
      - npm install typescript
      - npm install
  build:
    commands:
      - npx tsc --version
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: 'dist'
  discard-paths: yes